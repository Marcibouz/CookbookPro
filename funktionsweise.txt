-Binding Klasse am Anfang durch gradle erstellt. enthält Referenz auf gebindete XML:
 ActivityMainBinding-> activity_main.xml


-XML Pfad der Darstellung:

Mainactivity setzt contentview auf activity_main.xml
In activitymain.xml ist die toolbar und der content_main.xml definiert
in content_main.xml wird er nav_graph gehostet
Im navgraph wird das startfragment sowie die actions zwischen den Fragments beschrieben

-> In der .java Datei eines fragments kann man die onclick methode so stricken das basierend
auf der Direction-Klasse des Fragments welche auf dem nav_graph.xml basiert einen wechsel zu
einem anderen fragment oder ein senden von daten stattfinden kann.